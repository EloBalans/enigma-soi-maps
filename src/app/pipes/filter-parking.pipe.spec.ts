import { FilterParkingPipe } from './filter-parking.pipe';

describe('FilterParkingPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterParkingPipe();
    expect(pipe).toBeTruthy();
  });
});
