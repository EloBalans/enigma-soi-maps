import { FilterPoiPipe } from './filter-poi.pipe';

describe('FilterPoiPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPoiPipe();
    expect(pipe).toBeTruthy();
  });
});
